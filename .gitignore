# Python
__pycache__/
*.pyc

# Backtest is exploratory/regenerable — never commit it as if it were the record.
# The ONLY committed track record is the forward log: picks.csv + outcomes.csv.
backtest_results.csv
backtest_picks.csv
backtest_outcomes.csv
backtest_quality.csv
backtest_quality_asof.csv

# Generated brief (review locally; publishing needs G4 legal review)
brief.md
.vercel

# Phase 1: user hypotheses fetched fresh from Supabase each Action run (transient)
hypotheses/user_rules.json

# One-off patch scripts used to edit files in place; never part of the project.
patch*.py

# H-TSFM1 price cache. Re-downloadable, and restated by the vendor on every split.
# Committing it would create a SECOND price source alongside paths.csv that a later
# reader could mix with the sealed record — which is the split-adjustment trap itself.
# paths.csv stays the only committed price history.
tsfm_bars_cache.json
experiments/H-TSFM1-verdict.json.tmp
